import java.util.Comparator;

public class WorkPriorityStatus implements Comparator<String> {
    @Override
    public int compare(String o1, String o2) {
        if(o1.contains("")|| o2.contains(""))
            return 0;
        else if (o2.substring(1).compareTo(o1.substring(1))>1)
            return 1;
        else return -1;
    }
}
public class WorkPriorityManager{

    public static void main(String[] args)
    {
        Set<String> fullData=new TreeSet<String>(new WorkPriorityStatus());
        fullData.add("Severe");
        fullData.add("High");
        fullData.add("Low");
       System.out.println("My work priorities in descending order is-- " + fullData);

     }
}
